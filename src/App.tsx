import LeafletMap from "./components/leaflet-map/LeafletMap.tsx";

const App = () => {
    return (
        <div className="app">
            <LeafletMap/>
        </div>
    );
};

export default App;